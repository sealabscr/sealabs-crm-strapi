'use strict';

/**
 * organization-account controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::organization-account.organization-account');
